# D1 (bootstrap) subset — only what app/main.py, app/config.py, and
# app/database.py actually import/require at runtime. Later PRs (D2-D4,
# D5/D6) extend this file with their own additional dependencies
# (plaid-python, pyjwt, cryptography, psycopg2-binary, python-telegram-bot,
# openai, google-generativeai) as they land. O2 (DevOps) owns the final
# pinned version of this file.

fastapi
uvicorn[standard]
sqlalchemy[asyncio]
asyncpg
pydantic
pydantic-settings

# D4 (gateway) additions:
plaid-python
pyjwt
cryptography  # transitive: PyJWT needs this for ES256 verification (plaid_webhook.py)
psycopg2-binary  # transitive: sqlalchemy's postgresql+psycopg2 dialect (plaid_connector.py's sync engine), no direct import

# D5 (agent engine) additions:
openai
google-generativeai

# D6 (weekly digest job) additions:
python-telegram-bot  # async Bot.send_message, per R2's recommendation

# Q1 (QA, tests/** only) additions:
pytest
pytest-asyncio  # app/domains/finance/service.py's ingest_transactions is async
pytest-mock
httpx2  # transitive: fastapi.testclient.TestClient requires this (used in tests/gateway/test_router.py)
